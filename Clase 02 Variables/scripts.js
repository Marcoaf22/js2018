/*
*JS es dinamicamente tipado o debilmente tipado
*JS esta pabasdo en prototipos

 *Tipos de datos Primitivos
    ~string
    ~number (no diferencia entre float y number)
    ~boolean
    ~undefined
    ~null
    ~symbol
 *Tipos de datos Compuestos u Objetos
    ~arrys
    ~objetos
    ~maps
    ~sets
    ~funciones
 */

let a = "hola";
console.log(` a : ${typeof a}`);
a = 23;
console.log(` a : ${typeof a}`);

let indefinido;
if (!indefinido) consolgte.log("Si es indefinido");
